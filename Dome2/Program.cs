namespace Dome2
{

   public delegate int StringfuncDelegate(string str);
    
    internal class Program
    {
        static void Main(string[] args)
        {
            #region DELEGATE

            /*        int X = 5;
                    StringfuncDelegate stringfuncDelegate;


                    stringfuncDelegate = StringfuncDelegate.GetCountOfUpperChars

                    int Result = stringfuncDelegate.Invoke("omar ");

                    Console.WriteLine(Result);

                    stringfuncDelegate += StringfuncDelegate.GetCountOfLowerChars*/
            #endregion
            #region exsmaple

         /*   int[] numbers = { 10, 2, 3, 20, 15 };
            sortingALgorthms.Bubblesort(numbers, CompaerFunction.ComaperGrt);
            foreach (int Number in numbers)
                Console.WriteLine(Number); */
            #endregion


        }
    }
       
}
